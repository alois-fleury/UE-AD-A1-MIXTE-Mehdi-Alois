syntax = "proto3";

service Schedule {
    rpc GetScheduleByDate(Day) returns (ScheduleData) {}
}

message Day {
    string date = 1;
}

message ScheduleData {
    string date = 1;
    repeated string movies = 2;
}